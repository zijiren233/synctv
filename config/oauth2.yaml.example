# OAuth2/OIDC Configuration Example
#
# IMPORTANT: OAuth2 configuration is now part of the main config file!
#
# OAuth2 providers should be configured in config/config.toml under the [oauth2] section.
#
# See config/config.example.toml for the complete configuration format.
#
# Example for config/config.toml:
#
# [oauth2]
# providers = '''
# github:
#   client_id: "your_github_client_id"
#   client_secret: "your_github_client_secret"
#
# google:
#   client_id: "your_google_client_id"
#   client_secret: "your_google_client_secret"
#
# logto1:
#   type: logto
#   client_id: "logto1_client_id"
#   client_secret: "logto1_client_secret"
#   endpoint: "https://logto1.example.com"
# '''
#
# ============================================================================
# Supported Providers
# ============================================================================
#
# Standard OAuth2 Providers:
# - github: GitHub OAuth2
# - google: Google OAuth2
#
# Generic OIDC:
# - oidc: Generic OIDC provider with .well-known discovery
#   Supports issuer, auth_url, token_url, userinfo_url fields
#
# Chinese OAuth2 Providers (not yet implemented):
# - qq: QQ OAuth2
# - gitee: Gitee OAuth2
# - feishu: Feishu OAuth2
# - wechat: WeChat OAuth2
#
# Custom OIDC Providers (requires 'type: oidc' field):
# - Any OIDC-compliant provider (Casdoor, Keycloak, Authelia, etc.)
#
# ============================================================================
# Configuration Fields
# ============================================================================
#
# Common fields (all providers):
#   client_id: OAuth2 client ID
#   client_secret: OAuth2 client secret
#   redirect_url: (optional, auto-generated if not provided)
#
# Provider-specific fields:
#   logto:
#     endpoint: Logto server URL (e.g., "https://logto.example.com")
#
#   oidc:
#     issuer: Issuer URL for .well-known discovery
#     auth_url: (optional) Custom authorization endpoint
#     token_url: (optional) Custom token endpoint
#     userinfo_url: (optional) Custom userinfo endpoint
#
# ============================================================================
# Multiple Provider Instances
# ============================================================================
#
# To configure multiple instances of the same provider type:
# 1. Use different instance names (e.g., logto1, logto2)
# 2. Add 'type' field to specify the provider type
#
# Example:
#   logto1:
#     type: logto
#     endpoint: "https://logto1.example.com"
#     client_id: "xxx"
#     client_secret: "yyy"
#
#   logto2:
#     type: logto
#     endpoint: "https://logto2.example.com"
#     client_id: "aaa"
#     client_secret: "bbb"
#
# ============================================================================
# Environment Variables
# ============================================================================
#
# All YAML values can be overridden with environment variables:
#
# Format: SYNCTV_OAUTH2_<INSTANCE>_<FIELD>
#
# Examples:
#   SYNCTV_OAUTH2_GITHUB_CLIENT_ID=xxx
#   SYNCTV_OAUTH2_GITHUB_CLIENT_SECRET=yyy
#
#   SYNCTV_OAUTH2_LOGTO1_TYPE=logto
#   SYNCTV_OAUTH2_LOGTO1_ENDPOINT=https://logto1.example.com
#   SYNCTV_OAUTH2_LOGTO1_CLIENT_ID=xxx
#
# Note: Use single underscore (_) as separator, not double underscore (__)
#
# ============================================================================
# Callback URLs
# ============================================================================
#
# Callback URLs are automatically generated based on instance name:
#   http://<server.host>/api/oauth2/<instance_name>/callback
#
# Examples:
#   http://localhost:8080/api/oauth2/github/callback
#   http://localhost:8080/api/oauth2/logto1/callback
#   http://localhost:8080/api/oauth2/logto2/callback
#
# ============================================================================
# Security Notes
# ============================================================================
#
# 1. Tokens are NOT stored in database
#    - OAuth2 access/refresh tokens are only used temporarily during login
#    - They are discarded after fetching user info
#    - Only provider-user mappings are stored (for future logins)
#
# 2. No encryption key needed
#    - Because tokens are not stored, no encryption is required
#
# 3. HTTPS in production
#    - Always use HTTPS in production for OAuth2 callbacks
#    - Update server.host in config.toml accordingly
